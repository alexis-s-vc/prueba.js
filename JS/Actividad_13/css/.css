body{
    display: flex;
    flex-direction: column;
}
.gallery-container{
    width: 80vw;
    height: 100vh;
    background: none;
    position: fixed;
    top:0;
    padding: 0%;
    display: none;
}

.carousel{
    width: 100vw;
    height: 100vh;
    position: relative;
}

.carousel > ul{
    margin: 0;
    padding: 0 0%;
    list-style: none;
}
.slide{
    position: absolute;
    top: 10%;
    inset: 0;
    opacity: 0;
    transition: 200ms opacity easy-in-out;
    text-align: center;
    padding: 0%;
}

.slide > img{
    display: flex;
    justify-content:center;
    align-items: center;
    object-fit: cover;
    object-position: center;
    margin: 0;
    flex-shrink: 0;
    width: 100vw;
    height: auto;
}

.slide[data-active]{
    opacity: 1;
}

.carousel-button{
    position: absolute;
    z-index: 2;
    background: none;
    border: none;
    font-size: 4rem;
    top: 50vh;
    transform: translateY(-50%);
    color: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    border-radius: .25rem;
    padding: 0 .5rem;
    background-color: rgba(0, 0, 0, 0.1);
}
.carousel-button:hover, .carousel-button:focus{
    color: #1e0539b5;
    background-color: rgba(255, 255, 255, 0.6);
}

.carousel-button:focus{
    outline: 1px solid black;
}

.carousel-button.prev{
    left: 1rem;
}
.carousel-button.next{
    right: 1rem;
}